/**
 * Authetication type
 */
export const AuthType: string = 'jwt';

/**
 * Authentication Strategy
 */
export const AuthStrategy: string = 'jwt';

/**
 * Expiration in seconds
 * 3600 seconds = 1 hour
 */
export const ExpiresIn: number = 3600;

/**
 * Fort Knox Passwords - Secure enough for almost anything, like root or administrator passwords.
 * https://randomkeygen.com/
 *
 * Used two random keys (60) chars
 */
export const SecretOrPrivateKey: string = '9eN{g2}f$]:Tu_u(e13~ZWPjOu,=s@xTDBD4D)TI)-Y]zFRgqDUlt}nJ6]Sg';
